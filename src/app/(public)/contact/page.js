import { ContactPage } from "@/features/contact/contact-page";

export const metadata = {
  title: "Contact Us | AD Auto Parts",
  description:
    "Contact AD Auto Parts for support, compatibility guidance, and general enquiries.",
};

export default function ContactRoute() {
  return <ContactPage />;
}
