import { ReturnPolicyPage } from "@/features/static/policy-pages";

export const metadata = {
  title: "Return Policy | AD Auto Parts",
  description:
    "Read the AD Auto Parts return policy for used parts, compatibility responsibility, and refund review guidance.",
};

export default function ReturnPolicyRoute() {
  return <ReturnPolicyPage />;
}
